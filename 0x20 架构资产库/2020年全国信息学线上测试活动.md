#### T1 划拳

```cpp
#include <iostream>
#include <fstream>

using namespace std;

int k;
int a, b;

int main()
{
    freopen("hua.in", "r", stdin);
    freopen("hua.out", "w", stdout);	

	cin >> k;
    
    while (k--)
    {
        int x, y;
        cin >> x >> y;
        
        if (x == y) continue;
        
        if (x == 20 && (y == 5 || y == 10)) b += 10;
        else if (y == 20 && (x == 5 || x == 10)) a += 10;
        else
        {
            int t = x - y;
            if (t < 0) b -= t;
            else a += t;
        }
    }
    
    cout << a << ' ' << b << endl;
    
    return 0;
}
```

#### T2 打怪
```cpp
#include <iostream>
#include <fstream>
#include <algorithm>

using namespace std;

const int N = 5010;

struct Guai
{
    int attack;
    int life;
    
    bool operator< (const Guai& W)const
    {
        return life < W.life;
    }
}q[N];
int ans;

int main()
{   
    int n, w;
    cin >> n >> w;
    
    int A, B;
    cin >> A >> B;
        
    for (int i = 0; i < n; i++) cin >> q[i].attack >> q[i].life;

    sort(q, q + n);

    for (int i = 0; i < n; i++) 
    {
        if (A + B < q[i].attack) continue;
        
        if (w - q[i].life < 0) continue;
        
        w -= q[i].life;
        ans++;
        
        if (w == 0) break;
    }

    cout << ans << endl;

    return 0;
}

```

#### T3 楼梯
![分析过程](https://github.com/ZqIceberg/zqiceberg.github.io/blob/master/v2-2fbfb9e16be05fd4dcdb8d20dbc11166_r.jpg?raw=true)
```cpp
#include <iostream>
#include <cstdio>
#include <algorithm>
#include <cmath>

using namespace std;

double x, y, c;

bool check(double mid)
{
    double ry = sqrt(y * y - mid * mid);
    double rx = sqrt(x * x - mid * mid);

    double h = rx * ry / (rx + ry);

    if (h < c) return true; //h<c，间距取大了，会让梯子下滑，这样交点高度就小了
    else return false;
}

int main()
{
    scanf("%lf%lf%lf", &x, &y, &c);

    double l, r;
    if (x < y) l = 0, r = x; //取小的边长,作为右边界,因为梯子不能趴地上吧
    else l = 0, r = y;

    while (r - l > 1e-5) //答案保留小数点后3位，这里多取2位即可  POJ原题数据1e-4也能过
    {
        double mid = (l + r) / 2;
   
        if(check(mid)) r = mid;
        else l = mid;
    }

    printf("%.3lf\n", l);

    return 0;
}
```

#### T4 笨鸟
```cpp
//AC代码
#include <iostream>
#include <cstdio>
#include <fstream>

using namespace std;

typedef long long LL;

const int N = 5e5 + 10;

struct Point
{
	int x;
	int a, b;
}q[N];

int p[N]; //存向上飞最少点击次数
int ans;

int main()
{
	int n, X;
	cin >> n >> X;

	for (int i = 1; i <= n; i++)
		scanf("%d%d%d", &q[i].x, &q[i].a, &q[i].b);

	int up = 0, down = 0, last = 0;
	bool success = true;
	
	for (int i = 1; i <= n; i++)  //从1开始
	{
		int a = q[i].a;
		int b = q[i].b;
		int x = q[i].x;
		
		//更新一下，可以通过的区间
		a++, b--;         
		
		//上限，下限
		up += x - last;
                down -= x - last;
		
		//奇偶性调整，按 和 不按，两者之间的纵坐标，相差2。
                if ((up & 1) != (b & 1)) -- b;
                if ((down & 1) != (a & 1)) ++ a;
		
		up = min(up, b);
                down = max(down, a);
        
        if (up < down)
        {
            success = false;
            break;
        }
        
        // 公式up - (x - up) = y, up = (x + y) / 2;
        p[i] = (x + down) / 2;
        last = x;
	}
	
	if (!success) puts("Stupid bird!");
    else
    {
        for (int i = 1; i <= n; i ++ ) printf("%d\n", p[i]);
        printf("%d\n", p[n]);
    }

	return 0;
}

```